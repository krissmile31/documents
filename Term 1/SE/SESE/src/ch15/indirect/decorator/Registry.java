package ch15.indirect.decorator;

import utils.NotFoundException;

public class Registry {
  /**
   * @effects If there is a mapping for <code>o</code> in <code>this</code> returns
   *          the first string that is mapped to <code>o</code>.
   */
  public String reverseLookup(Object o) throws NotFoundException {
    return null;
  }
}
