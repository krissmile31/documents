package ch15.indirect.adaptor;

import utils.NotFoundException;

public class Registry {
  /**
   * @effects If there is a mapping for <code>s</code> in <code>this</code> returns
   *          the first object associated with <code>s</code> else throws
   *          <code>NotFoundException</code>
   */
  public Object lookup(String s) throws NotFoundException {
    return null;
  }
}
